#           计算机网络实验报告（四）

<br>

| 实验题目     | **端到端网络协议分析** |
| :----------- | :--------------------- |
| **报告日期** | **2025.12.23**         |
| **姓名**     | **熊宇祺**             |
| **学号**     | **09023112**           |

---

### 一. 实验目的

​	通过实验，巩固 Wireshark 网络抓包工具的使用方法，在真实网络环境下捕 捉和分析端到端网络应用数据包，掌握 HTTP、DNS、TCP、UDP 等应用层及传 输层端到端协议报文的结构，深入理解相关协议的特点与工作原理。 

### 二. 实验内容
1.  学习 Wireshark 网络抓包工具的基本操作，掌握**捕获过滤器**和**显示过滤器**的
使用方法。
2.  访问 Web 网站并捕捉数据包，分析 **HTTP 协议报文格式和交互过程**，观察
**TCP 协议报文格式**，分析**TCP 协议采用的可靠传输机制**、以及 **TCP 连接建立和
关闭的交互过程**。
3.  使用 **nslookup** 命令查询域名并捕捉数据包，分析 **DNS 协议报文格式和交互过程**，同时分析 **UDP 协议报文格式和交互过程**

### 三. 实验步骤（分析解释 + 系统截图）

#### 1. Wireshark基本操作与过滤器使用

Wireshark是本次实验的核心工具。其主界面主要分为三个窗格：
1.  **分组列表窗格 (Packet List Pane)**: 按时间顺序显示捕获到的每个数据包的概要信息。
2.  **分组详情窗格 (Packet Details Pane)**: 以协议分层的树状结构显示选定数据包的详细内容。
3.  **分组字节流窗格 (Packet Bytes Pane)**: 以十六进制和ASCII码显示数据包的原始数据。



![屏幕截图 2025-12-23 131000](C:\Users\alexa\Pictures\Screenshots\屏幕截图 2025-12-23 131000.png)

![屏幕截图 2025-12-23 131130](C:\Users\alexa\Pictures\Screenshots\屏幕截图 2025-12-23 131130.png)

​	在实验中，**显示过滤器**起到了至关重要的作用。它允许我们在捕获海量数据后，仅关注我们感兴趣的协议或会话，语法灵活，是网络分析的利器。例如，输入`dns`可以只查看DNS报文，输入`tcp.port == 80`可以只查看访问HTTP服务端口的TCP报文。

#### 2. HTTP协议报文格式和交互过程

**操作步骤**：

1.  **清理浏览器缓存**：在浏览器设置中或使用快捷键`Ctrl+Shift+Delete`清除“缓存的图片和文件”，以强制浏览器重新向服务器请求所有资源。
2.  **捕获与分析**：启动Wireshark，访问一个纯HTTP网站（`http://info.cern.ch`）。本次捕获到的是一个后台程序对 `connecttest.txt` 文件的请求。

**实验截图**：

![屏幕截图 2025-12-23 131345](C:\Users\alexa\Pictures\Screenshots\屏幕截图 2025-12-23 131345.png)

![屏幕截图 2025-12-23 131356](C:\Users\alexa\Pictures\Screenshots\屏幕截图 2025-12-23 131356.png)

HTTP（超文本传输协议)是应用层的核心协议之一，工作在TCP之上。

*   **HTTP请求报文 (Request, #1016)**:
    *   **请求行 (Request Line)**: `GET /connecttest.txt HTTP/1.1`
          *   `GET`: 请求**方法**，表示希望从服务器获取资源。
          *   `/connecttest.txt`: **URI** (统一资源标识符)，指明了请求资源的路径。
          *   `HTTP/1.1`: **协议版本**。
        *   **请求头部 (Headers)**:
            *   `Host:www.msftconnecttest.com`: 指明请求的目标服务器域名。
            *   `User-Agent:Microsoft NCSI`: 包含了发起请求的客户端（浏览器、程序）的信息。
            *   `Connection: Keep-Alive`: 建议服务器在完成本次响应后保持TCP连接，以便后续请求复用，减少连接建立的开销。
        *   **请求正文 (Body)**: 本次GET请求没有正文。
    
*   **HTTP响应报文 (Response, #1189)**:
    *   **状态行 (Status Line)**: `HTTP/1.1 200 OK`
        *   `HTTP/1.1`: 协议版本。
        *   `200`: **状态码**，表示请求已成功。
        *   `OK`: **原因短语**，对状态码的简要描述。
    *   **响应头部 (Headers)**:
        *   `Content-Type: text/plain`: 指明响应正文的媒体类型为纯文本。
        *   `Content-Length: 22`: 指明响应正文的长度（字节）。
        *   `Date: Mon, 17 Nov 2025 08:24:28 GMT`: 响应生成的时间。
    *   **响应正文 (Body)**: 在Wireshark的“Line-based text data”部分，我们可以直接看到`connecttest.txt`文件的内容。这清晰地证明了HTTP是**明文传输**的，任何中间节点都能轻易地读取传输内容，这也是HTTPS加密传输被广泛应用的原因。
    
#### 3. TCP协议分析
#### 3.1 定位并隔离TCP流

​	为了分析一个完整的TCP会话，而不是混杂在数千个无关报文中，我们使用了Wireshark的“追踪TCP流”功能。通过在一个已知的HTTP报文（#19号报文）上右键选择此功能，Wireshark自动生成过滤器 `tcp.stream eq 4`，将与该会话相关的所有TCP报文（从建立到断开）精确地隔离出来。

**实验截图**：

![屏幕截图 2025-12-23 132943](C:\Users\alexa\Pictures\Screenshots\屏幕截图 2025-12-23 132943.png)

![屏幕截图 2025-12-23 132958](C:\Users\alexa\Pictures\Screenshots\屏幕截图 2025-12-23 132958.png)



#### 3.2 TCP连接建立过程（三次握手报文）

在隔离出的TCP流的顶部，我们清晰地看到了连接建立的过程：

*   **第一次握手 (#19)**:
    *   **方向**: Client (`10.203.149.36`) -> Server (`180.184.103.12`)
    *   **报文分析**: 这是一个 `[SYN]` 报文，标志位中的`SYN`被置为1。客户端以此发起连接请求，并发送自己的初始序列号 `Seq=0`。此时，客户端TCP状态从`CLOSED`迁移到`SYN_SENT`。

*   **第二次握手 (#24)**:
    *   **方向**: Server -> Client
    *   **报文分析**: 这是一个 `[SYN, ACK]` 报文，标志位中的`SYN`和`ACK`都被置为1。服务器用`SYN`表示同意建立连接并发送自己的初始序列号`Seq=0`；用`ACK`（`Ack=1`，即客户端Seq+1）表示已成功收到客户端的请求。此时，服务器TCP状态从`LISTEN`迁移到`SYN_RCVD`。

*   **第三次握手 (#25)**:
    * **方向**: Client -> Server
    
    * **报文分析**: 这是一个 `[ACK]` 报文。客户端以此确认收到了服务器的`SYN`（`Ack=1`，即服务器Seq+1）。此报文发送后，客户端TCP状态迁移到`ESTABLISHED`；服务器收到此报文后，也迁移到`ESTABLISHED`状态。至此，可靠的双向连接建立完成。
    
      

#### 3.3 TCP连接关闭过程（四次挥手报文）

在TCP流的底部，我们观察到了连接的释放过程（本次由服务器主动发起）：

*   **第一次挥手 (#1607)**:
    *   **方向**: Server -> Client
    *   **报文分析**: 服务器发送一个 `[FIN, ACK]` 报文，`FIN`位置1，表示服务器已没有数据要发送，请求关闭从服务器到客户端方向的连接。服务器进入`FIN_WAIT_1`状态。

*   **第二次挥手 (#1608)**:
    *   **方向**: Client -> Server
    *   **报文分析**: 客户端收到`FIN`后，发送一个 `[ACK]` 报文作为确认。此时，从服务器到客户端的连接关闭，但从客户端到服务器的连接仍然可以发送数据。客户端进入`CLOSE_WAIT`状态。

*   **第三次挥手 (#1609)**:
    *   **方向**: Client -> Server
    *   **报文分析**: 客户端在确认自己也没有数据要发送后，也发送一个 `[FIN, ACK]` 报文，请求关闭从客户端到服务器方向的连接。客户端进入`LAST_ACK`状态。

*   **第四次挥手 (#1612)**:
    *   **方向**: Server -> Client
    *   **报文分析**: 服务器收到客户端的`FIN`后，发送最后一个 `[ACK]` 报文进行确认。服务器随即进入`TIME_WAIT`状态（等待2MSL以确保报文可靠送达），之后进入`CLOSED`。客户端收到这个`ACK`后，立即进入`CLOSED`状态。连接彻底释放。



#### 3.4 TCP连接时序图与状态变迁

**TCP三次握手时序图**

![屏幕截图 2025-12-23 133054](C:\Users\alexa\Pictures\Screenshots\屏幕截图 2025-12-23 133054.png)

**TCP四次挥手时序图 (服务器主动关闭)**

![屏幕截图 2025-12-23 133103](C:\Users\alexa\Pictures\Screenshots\屏幕截图 2025-12-23 133103.png)

注意：这里由于mermaid语法问题，无法画出斜线，实际每一个箭头的时间戳要晚于对应的箭尾的时间戳



#### 3.5 TCP协议报文格式

我们选择三次握手中的第一个报文（**#19 [SYN] 包**）进行详细的字段级分析。

**实验截图**：

![屏幕截图 2025-12-23 133422](C:\Users\alexa\Pictures\Screenshots\屏幕截图 2025-12-23 133422.png)

**TCP首部字段详细分析表**

| 字段 (Field)              | 捕获值 (Value) | 定义与含义分析                                               |
| ------------------------- | -------------- | ------------------------------------------------------------ |
| **Source Port**           | `57183`        | **源端口**。客户端操作系统为本次连接动态分配的端口号，用于标识本机上的通信进程。 |
| **Destination Port**      | `80`           | **目的端口**。HTTP服务的熟知端口号，指明了服务器上提供服务的应用程序。 |
| **Sequence Number**       | `0` (Relative) | **序列号**。本次连接的初始序列号(ISN)，标志着客户端发送数据流的起始位置。后续报文的序列号将在此基础上递增。 |
| **Acknowledgment Number** | `0`            | **确认号**。由于这是连接发起的第一个报文，ACK标志位为0，此字段无效。 |
| **Header Length**         | `32 bytes`     | **头部长度**。以4字节为单位，值为8（8*4=32）。因包含12字节的选项，故长度大于标准的20字节。 |
| **Flags**                 | `0x002 (SYN)`  | **标志位**。该字段的`SYN`位被置为1，明确表明这是一个**请求建立连接**的同步报文。 |
| **Window size**           | `65535`        | **窗口大小**。用于TCP流量控制。客户端以此告知服务器，其接收缓冲区当前最多能接收65535字节的数据。 |
| **Checksum**              | `0xbbda`       | **校验和**。用于检验TCP头部和数据在传输中是否出错，保障数据完整性。Wireshark 通常是在操作系统内核将数据包交给网卡之前捕获到这个数据包的，里面的校验和字段还是一个无效的占位符。所以Wireshark将其标识为**未验证** |
| **Options**               | (12 bytes)     | **选项**。用于在连接建立时协商一些高级特性：                 |
| *  - MSS                  | `1460 bytes`   | **最大报文段长度**。客户端声明其能接收的最大TCP数据段为1460字节，用于避免IP层分片。 |
| *  - SACK Permitted       | `1`            | **选择性确认许可**。表明客户端支持SACK机制，可在丢包时只重传丢失的报文段，提高效率。 |
| *  - Window Scale         | `256`          | **窗口扩大因子**。允许将窗口大小字段的值乘以256，以支持在高速网络下更大的接收窗口。 |



#### 3.6 TCP协议的可靠传输机制

​	TCP（Transmission Control Protocol，传输控制协议）是一种面向连接、面向字节流的传输层协议。为了在不可靠的 IP 网络之上提供可靠的数据传输服务，TCP 设计并采用了多种可靠传输机制，主要包括以下几个方面：

- 1. **序号与确认应答机制（Sequence Number & ACK）**

     TCP 将应用层传下来的数据视为连续的字节流，并为每一个字节分配一个序号（Sequence Number）。  

     接收方在成功接收数据后，会向发送方返回确认报文（ACK），其中确认号表示“期望接收到的下一个字节序号”。

     通过序号与确认号机制，TCP 可以：
     - 判断数据是否按序到达；

     - 发现数据是否丢失或重复；

     - 保证数据的有序交付。

- 2. **超时重传机制（Timeout Retransmission）**

     TCP 通过设置重传定时器（Retransmission Timer）来检测数据是否丢失。  
     如果发送方在规定时间内未收到对应数据段的确认报文（ACK），则认为该数据段可能在传输过程中丢失，发送方会对该数据段进行重传。

     该机制能够有效应对网络中的：
     - 分组丢失；
     - 分组延迟过大；
     - 确认报文丢失等情况

- 3. **差错检测机制（校验和）**

     TCP 在报文段首部中包含校验和字段（Checksum），用于检测数据在传输过程中是否发生比特错误。  发送方在发送数据前计算校验和，接收方在接收数据后重新计算并进行比对。若校验失败，接收方会丢弃该报文段，不发送确认，从而触发发送方的重传机制，保证数据的正确性。

- 4. **流量控制机制（Flow Control）**

     TCP 使用滑动窗口机制（Sliding Window）实现端到端的流量控制。  
     接收方通过通告窗口大小（Receive Window, rwnd）来告知发送方当前自身的接收能力，防止发送方发送数据过快而导致接收方缓冲区溢出。

     流量控制保证了：
     - 发送速率与接收能力相匹配；
     - 数据传输过程的稳定性。

- 5. **拥塞控制机制**
     
     虽然拥塞控制的主要目的是防止网络拥塞，但它也间接提高了 TCP 传输的可靠性。 
     TCP 通过慢开始、拥塞避免、快重传和快恢复等算法，动态调整发送速率，减少因网络拥塞导致的数据丢失。


#### 4. DNS协议报文格式和交互过程

**操作步骤**：

1.  **清空本地DNS缓存**：为确保能够捕获到完整的DNS查询过程，以管理员身份打开`cmd`，执行 `ipconfig /flushdns` 命令。
  
    ```powershell
    (base) PS C:\WINDOWS\system32> ipconfig /flushdns
    
    Windows IP Configuration
    
    Successfully flushed the DNS Resolver Cache.

  2.  **捕获与筛选**：启动Wireshark抓包，在浏览器中访问 `www.baidu.com`。停止抓包后，为精确定位，在显示过滤器中输入 `dns.qry.name == "www.baidu.com"`。

**实验截图**：

**#354号报文记录**

![屏幕截图 2025-12-23 134507](C:\Users\alexa\Pictures\Screenshots\屏幕截图 2025-12-23 134507.png)

**#360号报文记录**

![屏幕截图 2025-12-23 134517](C:\Users\alexa\Pictures\Screenshots\屏幕截图 2025-12-23 134517.png)

**结果详细分析**：
DNS协议负责将人类易于记忆的域名转换为机器能够识别的IP地址。从捕获结果看，一次域名解析通常包含对A记录（IPv4）和AAAA记录（IPv6）的并行查询。

**以A记录查询为例进行深度分析 (#354, #360号报文)**:

*   **DNS查询报文 (#354为例)**:
    - 客户端 10.203.149.36 向本地DNS服务器 172.19.2.1 发送了一个查询请求。根据分组列表窗格，#354号报文的Info列显示为 Standard query 0xc010 AAAA edge.microsoft.com。
    - **核心字段分析 (参照类似查询报文的结构)**:
      - **Transaction ID**: 0xc010。这是一个由客户端生成的16位随机数，作为本次“对话”的唯一标识。
      - **Flags**: 标记为Standard query，表明这是一个由客户端发起的标准查询请求。
      - **Questions**: 数量为1，表明该DNS报文中只包含一个问题。
      - **Queries (问题部分)**:
        - Name: edge.microsoft.com: 指明需要查询的域名。
        - Type: AAAA (IPv6 Address): 指明查询的记录类型为AAAA记录，即目标主机的IPv6地址。
    
*   **DNS响应报文 (#360为例)**:
    - DNS服务器 172.19.2.1 向客户端 10.203.149.36 返回了针对edge.microsoft.com的AAAA记录查询结果。这张截图的“分组详情”窗格为我们提供了极其丰富的分析信息。
    
    - **核心字段详细剖析**:
      - **Transaction ID**: 0xc010。与#354号查询报文的ID完全一致，客户端据此确认这是对它之前提问的回答。
      
      - **Flags**: 0x8180 (Standard query response, No error)。这是一个16位的标志字段：
        - 第一位为1 (8...)，表示这是一个**响应（Response）**报文。
        - Recursion Desired位为1，Recursion Available位为1 (...1..)，表明服务器支持并完成了递归查询。
        - RCODE为0 (...0 No error)，表示查询成功，没有任何错误。
        
      - **Counts (计数器)**:
        - Questions: 1: 响应中重复了一遍客户端提出的1个问题。
        - Answer RRs: 3: **回答资源记录(Resource Records)**的数量为3。这是本次响应的核心。
        - Authority RRs: 0 和 Additional RRs: 0: 授权和附加记录数为0。
        
      - **Answers (回答部分)**: 包含了3条资源记录，这揭示了一个**CNAME链**的解析过程：
        1. **第一条回答 (CNAME记录)**:
           - Name: edge.microsoft.com
           - Type: CNAME (Canonical Name)
           - CNAME: edge-microsoft-com.ax-0002.ax-msedge.net
           - **分析**: 它告诉客户端，你查询的edge.microsoft.com只是一个别名，它的“真名”是 edge-microsoft-com.ax-0002.ax-msedge.net。这是内容分发网络（CDN）的典型应用，通过别名将用户引导到CDN的边缘节点域名。
           
        2. **第二条回答 (CNAME记录)**:
           - Name: edge-microsoft-com.ax-0002.ax-msedge.net
           - Type: CNAME (Canonical Name)
           - CNAME: ax-0002.ax-msedge.net
           - **分析**: 进一步告诉客户端，上一个“真名”还是一个别名，最终的“真名”是ax-0002.ax-msedge.net。客户端必须沿着这个链条继续解析。
           
        3. **第三条回答 (AAAA记录)**:
           - Name: ax-0002.ax-msedge.net
           
           - Type: AAAA (IPv6 Address)
           
           - Address: 2620:1ec:33::11
           
           - **分析**: 这是最后一条回答，也是最终的回答。它提供了最终“真名”所对应的IPv6地址。在完成这一系列的CNAME跳转后，客户端终于获得了可用于建立网络连接的目标IP地址。这个过程完美地展示了现代大型网站如何通过多层别名来灵活地管理和分发流量。
           
             

#### 5. `nslookup` 查询域名并捕捉数据包

**操作步骤**：
在Wireshark中设置`dns`过滤器并抓包，同时在`cmd`中执行 `nslookup www.baidu.com`。

**实验截图**：

```powershell
(base) PS C:\WINDOWS\system32> nslookup www.baidu.com
Server:  UnKnown
Address:  172.19.2.1

Non-authoritative answer:
Name:    www.a.shifen.com
Addresses:  2408:873d:22:18cb:0:ff:b037:e6d8
          2408:873d:22:1a91:0:ff:b013:cf81
          153.3.238.127
          153.3.238.28
Aliases:  www.baidu.com
```

![image-20251223134836345](C:\Users\alexa\AppData\Roaming\Typora\typora-user-images\image-20251223134836345.png)

**分析**：
`cmd`的输出结果与Wireshark捕获的数据包形成了完美的对应。Wireshark的捕获揭示了`nslookup`命令在后台的真实动作：它是标准DNS协议的一个应用客户端。它发出了与浏览器解析时类似的A和AAAA记录查询，并且收到的响应（包括CNAME和IP地址）与`cmd`窗口中打印的结果完全一致。这深刻地揭示了上层工具与底层协议之间的实现关系。

**查询IPv4地址**

- **发出请求 (#69号报文):**
  - **动作:** 您的电脑 (10.203.149.36) 向本地 DNS 服务器 (172.19.2.1) 发送了一个标准的 DNS 查询。
  - **内容:** “请问 www.baidu.com 的 **A 记录** (IPv4 地址) 是什么？”
- **收到响应 (#70号报文):**
  - ![image-20251223134929920](C:\Users\alexa\AppData\Roaming\Typora\typora-user-images\image-20251223134929920.png)
  - **动作:** DNS 服务器返回了一个信息量丰富的响应。这个响应分为两部分，这与 nslookup 在屏幕上打印出的内容完全一致。
  - **第一部分 (别名):** 服务器首先回答：“www.baidu.com 只是一个**别名 (CNAME)**，它的真实规范名称是 www.a.shifen.com。”
  - **第二部分 (真实地址):** 紧接着，为了方便客户端，服务器直接提供了这个真实名称的 A 记录：“www.a.shifen.com 的 IPv4 地址是 153.3.238.127 和 153.3.238.28。”
  - **联动:** nslookup 会在屏幕上先打印出 CNAME 信息，再打印出最终的 IP 地址，源头就在这里。

**查询IPv6地址**

现代操作系统和应用为了支持 IPv6，通常会同时发起 A 和 AAAA 记录的查询。

- **发出请求 (#71号报文):**

  - **动作:** 电脑几乎在同一时间，又向 DNS 服务器发送了另一个查询。
  - **内容理解:** “请问 www.baidu.com 的 **AAAA 记录** (IPv6 地址) 是什么？”

- **收到响应 (#72号报文):**
- **动作:** DNS 服务器返回了与查询 A 记录时结构类似的响应。
  
- **第一部分 (别名):** 服务器再次告知：“www.baidu.com 的真实名称 (CNAME) 是 www.a.shifen.com。”
  
- **第二部分 (真实地址):** 然后它提供了真实名称的 AAAA 记录：“www.a.shifen.com 的 IPv6 地址是 2408:873d:22:18cb:0:ff:b037:e6d8和2408:873d:22:1a91:0:ff:b013:cf81。”



#### 6. UDP协议报文格式和交互过程

**6.1 实验步骤**

1. **产生流量**：在命令行（CMD）中输入 `nslookup www.baidu.com`。如图所示，系统向 DNS 服务器 `172.19.2.2` 发起了查询。

2. **捕获数据**：启动 Wireshark 捕获流量，并设置过滤器 `dns` 筛选相关数据包。

3. **选取样本**：在捕获列表中选取编号为 **Frame 714** 的数据包作为分析对象。

   **实验截图：**

   ![屏幕截图 2025-12-23 140821](C:\Users\alexa\Pictures\Screenshots\屏幕截图 2025-12-23 140821.png)

   ![屏幕截图 2025-12-23 140744](C:\Users\alexa\Pictures\Screenshots\屏幕截图 2025-12-23 140744.png)

   ![屏幕截图 2025-12-23 140716](C:\Users\alexa\Pictures\Screenshots\屏幕截图 2025-12-23 140716.png)

**6.2 UDP 报文格式分析**

根据实验截图（Frame 714），我们对 UDP 头部进行了详细分析。UDP 头部结构非常简洁，固定长度为 **8 字节**。

**UDP 头部字段详细数据表：**

| 字段名称 (Field)                   | 长度   | 实验捕获数值 | 含义与分析                                                   |
| :--------------------------------- | :----- | :----------- | :----------------------------------------------------------- |
| **Source Port**<br>(源端口)        | 2 字节 | **65245**    | 客户端（本机 `10.208.92.63`）随机开启的高位临时端口，用于接收服务器的回包。 |
| **Destination Port**<br>(目的端口) | 2 字节 | **53**       | 知名端口，标识接收方（服务器 `172.19.2.2`）运行的是 DNS 服务。 |
| **Length**<br>(长度)               | 2 字节 | **49**       | 表示 UDP 头部(8字节) + 数据载荷(Payload)的总长度。<br>计算可知 Payload = 49 - 8 = 41 字节。 |
| **Checksum**<br>(校验和)           | 2 字节 | **0x1567**   | 用于检测数据在传输中是否出错。状态显示 `[unverified]` 是因为网卡启用了硬件卸载功能，属正常现象。 |

> **实验观察**：
> 从 Wireshark 的 "Packet Details" 面板可以看出，UDP 协议直接承载在 IPv4 协议之上。相比于 TCP 协议，UDP 没有序列号、确认号、窗口大小等复杂控制字段，这验证了 UDP **低开销 (Low Overhead)** 的特点。

**6.3 UDP 交互过程分析**

通过观察 "Packet List" 面板，我们分析 UDP 的交互流程。

**1. 请求与响应时序：**

*   **请求包 (Request)**：
    *   **Frame No.**：714
    *   **时间**：63.259791
    *   **方向**：`10.208.92.63` (本机) $\rightarrow$ `172.19.2.2` (DNS服务器)
    *   **内容**：Standard query 0x6a2a ...
*   **响应包 (Response)**：
    *   **Frame No.**：715
    *   **时间**：63.261623
    *   **方向**：`172.19.2.2` (DNS服务器) $\rightarrow$ `10.208.92.63` (本机)
    *   **内容**：Standard query response 0x6a2a ...

**2. 交互特征分析：**

*   **无连接建立**：观察 Frame 714 发送之前，本机与服务器之间没有进行类似 TCP 的 "三次握手"（SYN 包），而是直接发送了 Query 数据。
*   **请求响应匹配**：由于 UDP 头部没有序列号，应用层依靠 **Transaction ID** 进行匹配。
    *   Frame 714 (请求) 的 Transaction ID 为 **0x6a2a**。
    *   Frame 715 (响应) 的 Transaction ID 同为 **0x6a2a**。
    *   这证明了 Frame 715 是针对 Frame 714 的回复。
*   **响应速度**：从发出请求到收到响应（63.261623 - 63.259791），耗时仅约 **1.8ms**，体现了 UDP 通信的高效性。


---

## 实验心得体会

### 1. 实验中遇到的问题及解决方法

在本次利用 Wireshark 进行端到端网络协议分析的过程中，我遇到了一些实际操作上的困难，但通过查找资料和反复尝试，最终找到了解决办法：

*   **问题一：高流量环境下难以定位目标报文。**
    *   **现象描述**：实验开始时，由于后台程序和系统服务（如微信、Windows Update）产生大量干扰流量，导致短短几秒内就捕获了数千个数据包，手动查找特定的 DNS 查询或 TCP 握手包极其困难。
    *   **解决方法**：我熟练掌握了 Wireshark **显示过滤器**的使用。
        *   对于 DNS 分析，使用 `dns.qry.name == "www.baidu.com"` 精确筛选。
        *   对于 TCP 分析，利用右键菜单中的 **"追踪流 (Follow TCP Stream)"** 功能，系统自动生成 `tcp.stream eq X` 过滤器，成功屏蔽了背景噪声，只保留了关注的那一条完整会话。
*   **问题二：抓包时无法捕获到完整的 HTTP 请求。**
    *   **现象描述**：在浏览器输入网址回车后，Wireshark 中只有 TCP 握手，却没有捕捉到 `GET` 请求，或者 DNS 查询没有发生。
    *   **解决方法**：分析认为是浏览器缓存和操作系统 DNS 缓存导致请求未真正发出。我使用了 `Ctrl+Shift+Delete` 清空浏览器缓存，并在命令行执行了 `ipconfig /flushdns` 命令，强制主机重新发起完整的解析和请求过程，随后成功捕获。

### 2. 实验中产生的错误及原因分析

在分析报文细节时，我观察到了一些看似“错误”的数据现象，经过深入分析，纠正了我的认知误区：

*   **现象：UDP/TCP 校验和（Checksum）显示为“Unverified”或错误。**
    *   **错误认知**：起初看到 Wireshark 在 UDP 头部（Frame 714）提示 `Checksum: 0x1567 [unverified]` 甚至标红时，我误以为是实验环境网络质量差导致数据传输出现了误码。
    *   **原因分析**：经过查阅资料，我了解到这是由于现代网卡的 **Checksum Offload（校验和卸载）** 机制。为了降低 CPU 负荷，操作系统将计算校验和的工作交给网卡硬件完成。Wireshark 捕获的是 CPU 发给网卡之前的原始数据，此时校验和尚未计算（通常为 0 或随机值），因此软件认为校验和错误。这属于正常现象，并非实验操作失误。
*   **现象：DNS 解析结果与预期不符（CNAME 链）。**
    *   **错误认知**：在使用 `nslookup` 时，我预期直接得到 `www.baidu.com` 的 IP 地址，但数据包中却先出现了 `www.a.shifen.com`。
    *   **原因分析**：这反映了我对 DNS 别名机制理解不够深入。大型网站为了 CDN 负载均衡，使用了 CNAME 记录。Wireshark 的 Answer 字段清晰地展示了从 `User Domain` -> `CNAME` -> `A/AAAA Record` 的完整解析链条，纠正了我“一个域名直接对应一个 IP”的简单线性思维。

### 3. 体会与收获

本次实验将枯燥的网络协议理论转化为了屏幕上真实跳动的字节，让我受益匪浅：

1.  **理论与实践的深度融合**：
    以前学习 TCP“三次握手”和“四次挥手”时只能死记硬背状态图，而现在通过 Wireshark 的时序图，我亲眼验证了 SYN、ACK、FIN 标志位的翻转以及序列号的递增逻辑。这种可视化的学习过程，让我对“可靠传输”的实现机制有了具象化的理解。

2.  **对协议特性的直观感悟**：
    通过对比 TCP 和 UDP 的报文结构，我深刻体会到了设计的权衡（Trade-off）：
    *   **TCP** 头部复杂（20+字节），握手过程繁琐，但换来了数据传输的可靠性。
    *   **UDP** 头部极简（8字节），无连接即发即走，完美契合了 DNS 这种对速度要求高、对丢包可容忍的应用场景。

3.  **工程素养与安全意识的提升**：
    在 HTTP 分析环节，看到文件名和 User-Agent 以**明文**形式呈现，让我受到极大的触动，深刻意识到 HTTP 协议的不安全性以及 HTTPS 加密的必要性。同时，掌握 Wireshark 这一工业级工具的使用，不仅提升了我的动手能力，更培养了我用“数据驱动”的思维去排查网络故障的工程素养。



